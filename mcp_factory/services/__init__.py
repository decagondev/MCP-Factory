"""Service plugin infrastructure for the MCP server.

This sub-package provides the base abstractions, service registry,
and concrete service implementations (e.g. APOD) that register
tools and resources onto the FastMCP instance.
"""
